- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.vimrc:
- create:
    - ~/Work

- shell:
    - [git submodule update --init --recursive, Installing submodules]
    - echo "hey, you guys"
    - sleep 2
    -
     command: ./test.sh
     stdout: true
     stderr: true

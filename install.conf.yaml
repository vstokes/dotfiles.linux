- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.vimrc:
- create:
    - ~/Work

- shell:
    - [git submodule update --init --recursive, Installing submodules]
    - echo "hey, you guys"
    - sleep 2
    # Below is the proper spacing
    # single bullet point for full command block.
    # command: ./test.sh
    # stdout: true
    # stderr: true
